# レビュー記録 — マニュアル・コードブロックのコピペ対応化（2026-08-07）

## 対象

ドキュメントのみの変更（アプリ本体ソース・テストは未変更）。`docs/manual/{en,ja}/` の各章コードブロックを、
「コピペしてそのまま動く」よう実ソースと一致させた。

- namespace / using の省略を補完
- 「Create `src/.../X.cs`:」と銘打たれた省略抜粋（bare メソッド・`/* ... */` プレースホルダ・
  class ラッパ欠如）を、実ファイルに一致する完全ファイルへ展開
- 序盤章の縮小版ブロックは、その段階のコードが参照しない using のみ落とす（段階的開示）が、
  提示状態でコンパイル可能であることを維持

変更ファイル（10）: `docs/manual/{en,ja}/` の 02 / 03 / 04 / 05 / 06。
合計 +416 / -174 行。掲載スタイルは B（章内=完全ファイル＋段階解説、章間=最小→成長）で確定（人の選択）。

## ゲート結果: 3役すべて PASS

| 役 | 判定 | 要点 |
|---|---|---|
| コードレビュアー | PASS | 各「Create X.cs:」ブロックは提示状態でコンパイル可能。using 集合が参照シンボルを過不足なくカバー（暗黙 using 込み）。完全ファイル版は実ソースと論理一致（差はコメント整理のみ）。en/ja はバイト一致。展開後の散文と矛盾なし。 |
| セキュリティレビュアー | PASS | 第2章の署名検証順序（生バイト読取→HMAC-SHA256検証→401/400→イベント処理）を維持。実シークレット漏れ無し（`demo-secret`/プレースホルダ LIFF id のみ）。第6章 `/reserve` の簡略化（`req.UserId` 信頼境界・`X-Forwarded-For`）はインラインコメントを落としたが散文で開示済み。トークンをログ出力しない点も実ソース同様。 |
| テスト・アーキ観点 | PASS | in-memory＋`I*Store`・遅延減衰・IAPポーリング・notifier→pushフォールバックの記述が実ソースと整合。第3章「試してみる」のテスト署名要約は実 `PetGrowthEngineTests.cs` の9メソッドと一致（過大カバレッジ主張なし）。段階的開示（ch3=IPetStoreのみ→ch6で3ストア追加、ch2 echo→ch4 dispatch→ch6 kibble）も一貫。 |

## 指摘（情報提供のみ・今回差分外）

- 第3章の `// Chapters 6–7 add IInventoryStore, IOrderStore, and INotifierTokenStore here.`
  （`03-pet-growth-engine.md` の登録プレースホルダ内コメント）は、実際は**第6章で3ストア全登録**なので
  「Chapter 6」が正確。**今回の差分では変更していない既存行**であり回帰ではない。将来の一括修正で
  1語直す候補（今回のドキュメント修正としては対応不要）。

## go / no-go

- レビューゲート: **3役 PASS**。
- 最終 go / no-go: **人（小林さん）待ち**。承認後にコミット／push する。
