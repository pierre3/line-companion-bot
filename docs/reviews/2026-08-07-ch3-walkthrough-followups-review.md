# レビュー記録 — 第3章 実地検証フォローアップ（2026-08-07）

## 対象

ドキュメントのみの変更（アプリ本体ソース・テストは未変更）。ユーザーが第3章を「ゼロから手順どおり」
実地検証中に判明した不足・要望への対応。変更ファイルは `docs/manual/{en,ja}/03-pet-growth-engine.md`
の2つのみ（+398 / -43）。

内容:
1. **`InMemoryPetStore.cs` のコードブロックを追加**（従来は散文説明のみ。これが無いと
   `AddInMemoryPersistence` が `InMemoryPetStore` を解決できずビルドが通らなかった）。
2. **「試してみる」のテストを、署名だけの要約（`/* ... */`）から実ファイル
   `PetGrowthEngineTests.cs` の全体へ差し替え**（コピペで `dotnet test` が通る形に）。
3. **エンジン解説を「設計判断とその理由」から「ペット育成の仕様＋各メソッドの役割」へ書き換え**
   （採用理由の語りとコード逐条を排し、仕様を表・箇条書きで提示）。

## ゲート結果: 3役すべて PASS

| 役 | 判定 | 要点 |
|---|---|---|
| コードレビュアー | PASS | `InMemoryPetStore.cs` / `PetGrowthEngineTests.cs` の両ブロックは実ファイルとバイト一致・提示状態でコンパイル可（using/namespace が全参照シンボルをカバー）。全 assert と `[InlineData]` を再計算して一致。en/ja コードはバイト一致。 |
| セキュリティレビュアー | PASS | 純粋なインメモリ・ドメイン状態＋決定的テスト＋散文。認証/トークン/署名/シークレット/ネットワークI/Oの影響面なし。シークレット値の混入なし（定数と合成テストユーザID `user-1` のみ）。 |
| テスト・アーキ観点 | PASS | 仕様表の各主張（0–100クランプ／Happinessの減衰がHungerより遅い＝1.0対2.0／feed・play・statusの効果／Xpは増加のみ／Stage帯 Lv1・2–4・5+／レア餌の全回復）が実コードと一致。メソッド役割の帰属も正確。テスト全体表示は実ファイルとバイト一致（過大カバレッジ主張なし）。 |

## 指摘への対応

- テスト・アーキの LOW nit（操作表 `status` の効果「changes nothing」は、実際は `Status` が減衰を
  精算し `LastInteractionUtc` を進める＝呼び出し側が `SaveAsync` で保存するため厳密には正しくない）を
  受け、表セルを **en: "shows the current state; no restore" / ja: "現在の状態を表示・回復なし"** に
  修正済み（本記録時点でコミット前に反映）。

## go / no-go

- レビューゲート: **3役 PASS**（nit 対応済み）。
- 人の go を得てコミット／push する。以降、第4章の実地検証へ移行。
